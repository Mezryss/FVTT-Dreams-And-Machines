<div class="w-full text-center text-xl text-emerald-800">
	{{#if store}}
		<span>{{ localize 'ChatMessages.StoreThreat' }}</span>
	{{else}}
		<span>{{ localize 'ChatMessages.SpendThreat' }}</span>
	{{/if}}
</div>
